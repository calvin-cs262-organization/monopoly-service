/**
 * Type definitions for the Monopoly service
 *
 * @author: samuelntadom
 * @date: Fall, 2025
 */

export interface Game {
  id: number;
  //   I think every other kind od data asides from id is returned as a string
  time: string;
}
